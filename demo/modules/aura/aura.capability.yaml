capability:
  name: aura
depends_on: [entity, movement]

properties:
  AuraSource:
    range: int32
    multiplier: float

requests:
  ActivateAura:
    source: EntityRef
    range: int32
    multiplier: float

  RefreshAuraEffect:
    source: EntityRef
    target: EntityRef
