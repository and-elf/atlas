# demo-interactable (issue #237): header-only (no .cpp - no hand-written
# logic, see interactable.hpp's own comment) - mirrors demo/modules/
# interruption/CMakeLists.txt's own precedent exactly.
atlas_add_demo_capability(NAME interactable)
